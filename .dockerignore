node_modules
dist
build
coverage
.nyc_output
# Must not enter the image: dist/ is excluded above, so a stale cache makes tsc
# report everything up to date and emit nothing.
*.tsbuildinfo

.git
.gitignore
.gitnexus

# Secrets — never bake into an image; pass at runtime.
.env
.env.*

*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

.DS_Store
.idea
.vscode
.claude

Dockerfile
.dockerignore
docker-compose.yml

test
memory
supabase
CLAUDE.md
README.md
package-lock.json
